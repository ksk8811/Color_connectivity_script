function [vol ] = do_fsl_compare_roi_vol(fl)
%


for k=1:length(f)

    ff= cellstr(char(f(k))
    vol = do_fsl_getvol(ff);
    
    tmp=tempname;
    
    do_fsl_mult(ff,tmp)
    
    volinterp = do_fsl_getvol(tmp)
    
    keyboard
        
end


